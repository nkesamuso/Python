# Python workspace

This repository contains small example scripts.

Dependencies
- Install required packages:

```powershell
python -m pip install -r requirements.txt
```

Or install individually, for example:

```powershell
python -m pip install pandas numpy
```
